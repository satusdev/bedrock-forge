# Environment variables for scripts/provision-cyberpanel-bedrock.sh
# Copy this file to scripts/.env.provision and fill in your actual values.

# CyberPanel Server Details
SERVER_IP="YOUR_SERVER_IP_ADDRESS"
SSH_USER="root" # User for SSH connection (usually root for CyberPanel CLI)
SSH_PRIVATE_KEY="/path/to/your/ssh/private_key" # Absolute path to the SSH private key for passwordless login

# Cloudflare Details
CLOUDFLARE_API_TOKEN="YOUR_CLOUDFLARE_API_TOKEN" # Cloudflare API Token with DNS edit permissions
CF_ZONE_ID="YOUR_CLOUDFLARE_ZONE_ID" # The Zone ID of the domain in Cloudflare

# CyberPanel Site Configuration
ADMIN_EMAIL="admin@example.com" # Email address for the CyberPanel website owner (can be anything)
PHP_VERSION="8.1" # Desired PHP version for the new site (e.g., 8.0, 8.1, 8.2)

# Note: Database credentials (DB_NAME, DB_USER, DB_PASSWORD) are generated by the script.
# Note: WordPress admin credentials are now handled by the manage-site.sh script.
