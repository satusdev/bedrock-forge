# Example Environment File for Core Services (Shared Database)
# Copy this to core/.env and set a secure password.
# This file is loaded by core/docker-compose-db.yml

# Required: Set a secure root password for the shared MySQL database container.
# This password is used by scripts like create-site.sh (when using --create-db)
# to manage databases and users within the shared MySQL instance.
MYSQL_ROOT_PASSWORD=changeme_in_core_dotenv_or_pass_via_cli
